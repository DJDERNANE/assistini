<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PDF Template</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
        }
        h1 {
            color: #3498db;
        }
        p {
            font-size: 14px;
            line-height: 1.6;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .footer {
            position: fixed;
            bottom: 0;
            text-align: center;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>
<body>
    <h1><%= provider_name %></h1>
    <p>Invoice N° : <%= invoiceId %></p>
    <h5>
        <%= to %>
    </h5>

    <table>
        <tr>
            <th>Services</th>
            <th>Price</th>
        </tr>
        <% services.forEach(service => { %>
        <tr>
            <td><%= service.nom %></td>
            <td><%= service.price %></td>
        </tr>
        <% }) %>
    </table>

    <h1>
        Total : <%= total %>
    </h1>
</body>
</html>
